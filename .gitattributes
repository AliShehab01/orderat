# Keep LF on every OS: Deno and Node reject a CRLF shebang line when a script is imported
# (vitest failed on scripts/*.mjs after a Windows checkout with core.autocrlf=true).
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
